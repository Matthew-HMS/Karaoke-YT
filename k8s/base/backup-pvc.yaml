apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: singalong-backups
  namespace: karaoke
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: local-path
  resources:
    requests:
      # Holds the rolling daily snapshots (gzipped, tiny). Plenty of headroom.
      storage: 2Gi
